# Exercise 1: Mocking a Service Dependency in a Controller Test

## Description
This exercise covers unit testing a Spring REST Controller by mocking the `UserService` dependency using Mockito's `@MockBean` and verifying JSON endpoints with `MockMvc`.

## How to Compile & Run

### Run Tests
From this directory, run:
```cmd
d:\Study\Cognizant\apache-maven-3.9.6\bin\mvn.cmd test
```
