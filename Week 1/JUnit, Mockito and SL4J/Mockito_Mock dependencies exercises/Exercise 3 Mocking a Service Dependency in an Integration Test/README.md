# Exercise 3: Mocking a Service Dependency in an Integration Test

## Description
This exercise covers writing a full integration test with `@SpringBootTest` and `@AutoConfigureMockMvc` while stubbing a specific service bean (`UserService`) using `@MockBean`.

## How to Compile & Run

### Run Tests
From this directory, run:
```cmd
d:\Study\Cognizant\apache-maven-3.9.6\bin\mvn.cmd test
```
