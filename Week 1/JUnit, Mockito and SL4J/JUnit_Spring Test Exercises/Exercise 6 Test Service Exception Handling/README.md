# Exercise 6: Test Service Exception Handling

## Description
This exercise covers writing service-layer unit tests in Spring Boot that assert that a custom exception (e.g., `UserNotFoundException`) is thrown when querying missing resources, utilizing `assertThrows()`.

## How to Compile & Run

### Run Tests
From this directory, run:
```cmd
d:\Study\Cognizant\apache-maven-3.9.6\bin\mvn.cmd test
```
